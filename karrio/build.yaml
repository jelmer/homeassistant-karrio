---
# python:3.12-slim is multi-arch (amd64 + arm64), and the karrio packages
# we install from PyPI are pure Python — so the same Dockerfile builds on
# both arches.
build_from:
  aarch64: python:3.12-slim
  amd64: python:3.12-slim
args:
  KARRIO_VERSION: "2026.1.32"
  BASHIO_VERSION: "0.18.1"
